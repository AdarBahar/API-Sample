# Core API framework
fastapi>=0.104.1
uvicorn[standard]>=0.24.0

# Data processing
pandas>=2.1.0
numpy>=1.24.0

# Type hints and validation (included with fastapi but explicit for clarity)
pydantic>=2.4.0